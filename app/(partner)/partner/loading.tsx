import { DeskLoading } from "@/components/portal/desk-loading";

/** 🔴 C19: the partner portal, while a page is fetched. See `DeskLoading`. */
export default function PartnerLoading() {
  return <DeskLoading />;
}
