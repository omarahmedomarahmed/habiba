import { RouteLoading } from "@/components/patient/route-loading";

/** 🔴 P19: the patient app, while a page reads what it shows. The chrome keeps its own orb. */
export default function PatientLoading() {
  return <RouteLoading />;
}
