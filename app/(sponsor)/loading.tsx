import { DeskLoading } from "@/components/portal/desk-loading";

/** 🔴 C19: the company portal, while a page is fetched. See `DeskLoading`. */
export default function SponsorLoading() {
  return <DeskLoading />;
}
