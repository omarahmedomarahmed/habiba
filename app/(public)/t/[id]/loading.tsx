import { PageLoading } from "@/components/public/page-loading";

/** 0165 / B35: a profile reads the database first. See the component. */
export default function ProfileLoading() {
  return <PageLoading />;
}
