import { PageLoading } from "@/components/public/page-loading";

/** 0165 / B35: the radar reads the database first. See the component. */
export default function RadarLoading() {
  return <PageLoading />;
}
