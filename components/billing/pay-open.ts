/**
 * 🔴 Board 475: the pending bar asks a payment sheet that is already on the
 * page to open. Its own module, so the bar in every layout does not carry the
 * sheet's code along with the name of one event.
 */
export const PAY_OPEN_EVENT = "pay-open";
