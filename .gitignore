node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env*.local

*.log
npm-debug.log*
.DS_Store
.vercel
*.tsbuildinfo
next-env.d.ts

# Generated demo assets — regenerate with scripts/demo-video.mts
demo-output/

# Development-only credential uploads (see lib/uploads.ts)
.uploads/

# 18.12 — admin screenshots are swept on demand and never committed. An admin
# console shows many patients at once and is a map of the whole system; treat
# this repository as if it will be public one day.
docs/screens/admin/
# Playwright storage states for the screens sweep. Session cookies.
.screens/
.render/

# Synthesised eval audio. Regenerated on demand; never committed.
.evals-audio/

# C80 — admin console screens are swept from the walkthrough, never committed.
docs/walkthrough-2/admin_*
# The walkthrough harness itself: browser state, captured invite links, and the
# sweep it produced. The report and the screenshots are the record.
.walkthrough2/state-*.json
.walkthrough2/invite.txt
.walkthrough2/sweep.json
.verify-tmp-*/
docs/walkthrough-2/frames/*/*admin*

# ── walkthroughs and the simulation ──────────────────────────────────────────
#
# The first two walkthroughs' frames were deleted on 2026-09-14 and stay deleted: 94MB of
# screenshots of two older versions of the product, which a third pass would have been read
# against. Their WRITTEN record is committed, in docs/walkthrough-archive/.
docs/walkthrough/
docs/walkthrough-2/

# 🔴 THE SIMULATION'S FRAMES ARE COMMITTED, DELIBERATELY, and that is the exception rather
# than a lapse. They are the proof that six months of this product was operated rather than
# described, and they are the raw material for the films. docs/simulation/04-CAPTURE.md
# caps them at roughly 600 and says what to photograph; past that, the capture is
# photographing states rather than people.
#
# 🔴 EXCEPT THE OPERATOR'S, WHICH IS C80 AND DOES NOT BEND. An admin console shows many
# people at once, and this repository is treated as if it will be public one day. The
# operator agent's frames are taken, read, and never committed.
docs/walkthrough-3/**/op-*/
docs/walkthrough-3/**/*admin*

