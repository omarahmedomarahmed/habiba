# Every lifecycle in the product

Generated by `npm run lifecycles -- --write`. Do not edit by hand: it is derived from `lib/lifecycle/machines.ts`, which `verify:machines` holds against `lib/db/schema.ts` and against the real route list.

**11 lifecycles · 48 states · 51 transitions**

---

## payout

Table `payout_requests`. Starts at **requested**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `requested` | in flight | /earnings, and /admin/payouts | 3 days before it reaches us with or without the clinic |
| `approved` | in flight | /admin/payouts | 2 days to send |
| `sent` | in flight | /earnings | 5 days to land |
| `confirmed` | finished | /earnings | not a wait |
| `rejected` | STOPPED, this one is over | /earnings | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `requested` | `approved` | staff approves | staff |
| `requested` | `rejected` | staff rejects with a reason | staff |
| `approved` | `sent` | staff marks the transfer made | staff |
| `sent` | `confirmed` | the clinician confirms receipt | clinician |

## session

Table `sessions`. Starts at **scheduled**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `scheduled` | in flight | /patient/sessions and /sessions | until it starts |
| `in_progress` | in flight | the room | not a wait |
| `completed` | finished | /patient/sessions and /notes | not a wait |
| `cancelled` | STOPPED, this one is over | /patient/sessions | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `scheduled` | `in_progress` | somebody joins | clinician |
| `scheduled` | `cancelled` | either side cancels | patient |
| `in_progress` | `completed` | the room ends | clinician |
| `in_progress` | `cancelled` | abandoned | time |

## invoice

Table `invoices`. Starts at **due**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `due` | in flight | /billing | the allowance before service is interrupted |
| `paid` | finished | /billing | not a wait |
| `included` | finished | /billing | not a wait |
| `waived` | finished | /billing | not a wait |
| `void` | finished | /billing | not a wait |
| `failed` | STOPPED, can still move | /billing | until the allowance runs out |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `due` | `paid` | the payer pays | clinician |
| `due` | `failed` | the charge is refused | system |
| `due` | `waived` | staff waives it | staff |
| `due` | `void` | staff voids it | staff |
| `due` | `included` | credit or a seat covers it | system |
| `failed` | `paid` | the payer pays another way | clinician |
| `failed` | `void` | staff voids it | staff |

## payment

Table `payments`. Starts at **pending**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `pending` | in flight | the bar that follows an unfinished payment around the product | hours, and the bar says so |
| `paid` | resting | the same bar, green | not a wait |
| `refunded` | finished | /billing | not a wait |
| `failed` | STOPPED, this one is over | /billing | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `pending` | `paid` | an operator confirms the transfer | staff |
| `pending` | `failed` | an operator rejects the proof | staff |
| `paid` | `refunded` | staff refunds | staff |

## verification

Table `therapist_verifications`. Starts at **draft**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `draft` | in flight | /onboarding | not a wait |
| `submitted` | in flight | /onboarding | 2 working days |
| `approved` | finished | /dashboard | not a wait |
| `rejected` | STOPPED, can still move | /onboarding | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `draft` | `submitted` | documents sent | clinician |
| `submitted` | `approved` | staff approves | staff |
| `submitted` | `rejected` | staff rejects with a reason | staff |
| `rejected` | `submitted` | fresh documents sent | clinician |

## claim

Table `patient_claims`. Starts at **pending**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `pending` | in flight | /patient/claim | the life of the link |
| `verified` | finished | /patient | not a wait |
| `rejected` | STOPPED, this one is over | /patient/claim | not a wait |
| `expired` | STOPPED, this one is over | /patient/claim | not a wait |
| `locked` | STOPPED, can still move | /patient/claim | the lockout window |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `pending` | `verified` | the patient proves the code | patient |
| `pending` | `rejected` | the clinician withdraws it | clinician |
| `pending` | `expired` | the link ages out | time |
| `pending` | `locked` | too many wrong codes | system |
| `locked` | `pending` | the lockout ends | time |

## grant

Table `benefit_grants`. Starts at **pending**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `pending` | in flight | /patient/benefit | until the employer answers |
| `granted` | resting | /patient/benefit | not a wait |
| `rejected` | STOPPED, this one is over | /patient/benefit | not a wait |
| `revoked` | STOPPED, this one is over | /patient/benefit | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `pending` | `granted` | the employer approves | company |
| `pending` | `rejected` | the employer refuses | company |
| `granted` | `revoked` | the employer ends cover | company |

## sponsor

Table `sponsors`. Starts at **held**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `held` | in flight | the company portal, with every page saying what is waiting | until we have called them |
| `active` | resting | /sponsor | not a wait |
| `suspended` | STOPPED, can still move | /sponsor | 1 working day to a decision, either way |
| `closed` | finished | /sponsor | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `held` | `active` | staff approve after speaking to them | staff |
| `active` | `suspended` | staff suspend | staff |
| `suspended` | `active` | staff restore | staff |
| `active` | `closed` | the company closes the account | company |

## renewal

Table `subscription_renewals`. Starts at **due**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `due` | in flight | /billing | the allowance before service is interrupted |
| `paid` | finished | /billing | not a wait |
| `void` | finished | /billing | not a wait |
| `lapsed` | STOPPED, can still move | /billing | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `due` | `paid` | the payer pays | clinician |
| `due` | `lapsed` | the allowance runs out | time |
| `due` | `void` | staff void it | staff |
| `lapsed` | `paid` | the payer pays late | clinician |

## presence

Table `therapist_radar`. Starts at **offline**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `offline` | resting | /dashboard | not a wait |
| `online` | in flight | the radar, and /dashboard | not a wait |
| `pending` | in flight | /dashboard | minutes, before the hold is released |
| `in_session` | in flight | the room | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `offline` | `online` | the clinician goes on the radar | clinician |
| `online` | `offline` | the clinician leaves it | clinician |
| `online` | `pending` | a patient asks for them | patient |
| `pending` | `in_session` | the clinician accepts | clinician |
| `pending` | `online` | the hold is not taken up | time |
| `in_session` | `online` | the session ends | clinician |
| `online` | `offline` | the sweep finds them gone | time |

## slot

Table `availability_slots`. Starts at **open**.

| State | What it is | Seen on | For how long |
| --- | --- | --- | --- |
| `open` | in flight | the clinician's calendar and the booking sheet | not a wait |
| `held` | in flight | the booking sheet | minutes, while a payment is finished |
| `booked` | resting | both calendars | not a wait |
| `blocked` | resting | the clinician's calendar | not a wait |

| From | To | What does it | Who |
| --- | --- | --- | --- |
| `open` | `held` | a patient starts booking | patient |
| `held` | `booked` | the payment completes | patient |
| `held` | `open` | the hold expires | time |
| `open` | `blocked` | the clinician blocks the hour | clinician |
| `blocked` | `open` | the clinician frees it | clinician |
| `booked` | `open` | the session is cancelled | patient |

---

## Every way a person is stopped

This is the register. `STOPPED, can still move` is the one to watch: the person is blocked and the row has not ended, so whether they are stuck or merely delayed depends entirely on who owns the way out.

| Lifecycle | State | Which | What they can do | Who holds the key |
| --- | --- | --- | --- | --- |
| payout | `rejected` | STOPPED, this one is over | the reason is shown and a corrected request can be made | nobody, it is over |
| session | `cancelled` | STOPPED, this one is over | rebook from the same screen | nobody, it is over |
| invoice | `failed` | STOPPED, can still move | the reason is shown and the payment can be retried | clinician, staff |
| payment | `failed` | STOPPED, this one is over | the reason is shown and another attempt can be made | nobody, it is over |
| verification | `rejected` | STOPPED, can still move | the reason is shown and fresh documents can be sent | clinician |
| claim | `rejected` | STOPPED, this one is over | the reason is shown and the clinician can send a fresh link | nobody, it is over |
| claim | `expired` | STOPPED, this one is over | ask the clinician for a new link, from that screen | nobody, it is over |
| claim | `locked` | STOPPED, can still move | too many attempts; it unlocks on its own and says when | time |
| grant | `rejected` | STOPPED, this one is over | the employer's reason is shown, and the patient can still pay for themselves | nobody, it is over |
| grant | `revoked` | STOPPED, this one is over | cover has ended; the patient can still pay for themselves | nobody, it is over |
| sponsor | `suspended` | STOPPED, can still move | the reason is shown, with who to contact, and staff can restore it | staff |
| renewal | `lapsed` | STOPPED, can still move | the plan drops to pay as you go and the screen says so | clinician |

## Every promise we have made

A promise is how long a state may last before somebody must act. Task #166 alerts on the AGE of the oldest row in each of these, never on the count, because a queue of two that has been waiting a week is the problem and a queue of two hundred cleared hourly is not.

| Lifecycle | State | Promise | A clock can count it |
| --- | --- | --- | --- |
| payout | `requested` | 3 days before it reaches us with or without the clinic | yes |
| payout | `approved` | 2 days to send | yes |
| payout | `sent` | 5 days to land | yes |
| session | `scheduled` | until it starts | yes |
| invoice | `due` | the allowance before service is interrupted | yes |
| invoice | `failed` | until the allowance runs out | yes |
| payment | `pending` | hours, and the bar says so | yes |
| verification | `submitted` | 2 working days | yes |
| claim | `pending` | the life of the link | yes |
| claim | `locked` | the lockout window | yes |
| grant | `pending` | until the employer answers | **no, and #166 needs one** |
| sponsor | `held` | until we have called them | **no, and #166 needs one** |
| sponsor | `suspended` | 1 working day to a decision, either way | yes |
| renewal | `due` | the allowance before service is interrupted | yes |
| presence | `pending` | minutes, before the hold is released | yes |
| slot | `held` | minutes, while a payment is finished | yes |

