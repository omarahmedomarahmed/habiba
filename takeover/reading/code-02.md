# Slice 02: data-a-f

## Files

### lib/data/actuals.ts (640 lines)
- For: `/admin/actuals`, the company's measured monthly result (revenue, spend, bank, held-for-others, runway). super_admin only, never clinical.
- Decides: `monthlyActuals` (229) builds the month range from six sources, earliest wins (`monthRange` 473); revenue is `-SUM` of `platform_revenue` split by `ref_type` (`ledgerByMonth` 567, 593 to 609); completed sessions only (`activityByMonth` 540); AI cost rounded once (309, `CENTS_PER_MICRO = 1000`, consistent with H13: 1000 units per cent); `oursCents = bank - held` (356); burn over trailing three months, null when profitable (`positionFrom` 425 to 432); runway floors negative "ours" at zero (432).
- Assumes: ledger sign convention (positive = debit) from `lib/billing/ledger.ts`; `capitalByMonth` returns a CUMULATIVE figure per month (it is used as "capital in by end of month" at 329); `payrollByMonth` in `lib/data/payroll.ts`.
- Promises: none of P1..A5 directly. Reads `sessions` counts only (no names). Not clinical.
- Notes: `monthRange` counts FORWARD from the first month and stops at `maxMonths` (496), so once the business is older than 36 months the page shows the first 36 and never the current month, while the running cash also stops accumulating. Ledger months outside the range are silently dropped from every total. `NOT_MEASURED_HERE` (57) is printed on the page; its payroll text hardcodes "two of the seven on the payroll are founders drawing the same $500" (70), a data claim frozen in source.

### lib/data/admin-team.ts (80 lines)
- For: `createBackOfficeUser`, minting `staff` / `manager` accounts in our own org (slug `24therapy`).
- Decides: role enum closed to staff|manager (53), super_admin not creatable (comment 28), org looked up by slug (57), password through `validatePassword` (50). Any insert failure is reported as "already an account with that email" (74 to 76), which hides every other failure (a CHECK, a missing column).
- Assumes: caller is behind `requireRole("super_admin")` (not checked here); `users.email` unique.
- Promises: A5 (role is a list): serves it by making non-founder accounts exist. No audit written in this function; caller must.
- Notes: logs email via `ref()` (hashed ref), fine.

### lib/data/admin.ts (468 lines)
- For: cross-organisation admin reads (stats, clinician list, one clinician in full, audit log, AI usage) plus `setUserStatus` and `setVerification`.
- Decides: header (35) says every function must be behind `requireRole("super_admin")`; nothing in the file checks it. `therapistPatients` (291) returns patient names, emails, phones for a clinician; `therapistSessions` (343) patient names and session times; `therapistCopilotUsage` (373) counts only; `listAuditLog` (193) deliberately does not join patients, joins the three actor tables (users, sponsor_users, clinic_managers). AI cost from `cost_microcents / 1000` rounded once (74, 417, 447).
- Assumes: callers audit. Checked: `app/(admin)/admin/therapists/[id]/page.tsx:40,48` does `requireRole("super_admin")` then a `break_glass` audit before reads; `app/(admin)/admin/actions.ts:45,59` audits suspend and verify.
- Promises: A5 (every read written down): kept for the one-clinician page. Not a sponsor/clinic surface.
- Notes: `platformStats.clinicians` (50) counts every `users` row including staff, managers and super_admins, labelled clinicians. `listClinicians` (101) and `therapistOverview` (259) left-join `subscriptions` on organisation, so an organisation with more than one subscription row duplicates the clinician row. `setVerification` (146) writes `users.verification_status` first; migration `drizzle/0083_verification_one_truth.sql:108 to 121` forces that column to the derived value on every update, so the first write is a no-op and the comment "written to both places" (132 to 139) is stale. It sets `reviewedAt` even for "pending" / "unverified" (178), so an admin resetting to pending looks reviewed.

### lib/data/assessments.ts (598 lines)
- For: questionnaires (PHQ-9 style instruments): publish, assign, answer with per-answer timing, score, band for clinician only.
- Decides: `publishInstrument` (82) refuses `licence = 'licensed'` (94, also DB `instruments_free_only`) and a translated instrument nobody reviewed (108). `ownedAssignment` (182) scopes patient calls to `patients.person_id`. `recordAnswer` (199) refuses a completed assignment, a question or value not on the instrument (229 to 235), clamps timing to 5 minutes or null (169, 237). `completeAssignment` (290) sums whatever answers exist and freezes. `bandFor` (330) separate so a patient screen never gets a band. `historyForPerson` (462) returns score and date only.
- Assumes: the clinician caller has checked access (`app/(app)/patients/[id]/assessments/actions.ts:27` `gate` does `getPatient` plus `accessFor != revoked`); patient callers pass `personId` from the session (`app/(patient)/patient/assessments/actions.ts:28,51`).
- Promises: P3 in spirit (no machine verdict to a patient: score without band). T5 not applicable (not copilot).
- Notes: `completeAssignment` does not check that every question was answered (298 to 303): a PHQ-9 finished after three answers freezes a low score on the chart with status completed, and `finishAssessment` only checks for null. `assignInstrument` (125) does not check the `sessionId` belongs to this patient. The comment at 282 says a score must keep meaning what it meant on the day, but the clinician's `pollAssessment` and `timingsFor` (`app/(app)/patients/[id]/assessments/actions.ts:102,141`) band and label questions with `instrumentByKey`, the LATEST published version, not the version the assignment names (`instrumentVersion`, 141).

### lib/data/capital.ts (326 lines)
- For: capital contributions and typed-in other costs for `/admin/actuals`. super_admin only.
- Decides: `mustBeFounder` (82) throws unless `actor.role === "super_admin"` on every read and write. `capitalByMonth` (132) is cumulative and adds pre-range money (159 to 166). `setOtherCost` (262) one figure per kind per month, zero deletes (291), audits old and new amounts. `removeCapital` (225) hard deletes, audited with both figures.
- Assumes: `audit()` tolerates a non-uuid `resourceId` (318 passes `kind:month`); it does: `lib/audit.ts:129,130` routes non-uuid to `resourceKey` (H6 handled).
- Promises: A5 (every write written down): kept here.
- Notes: the pre-range block (151 to 166) says `monthRange` starts at the first thing in the product; `lib/data/actuals.ts:480` now includes `capital_contributions` in that range, so `before` is always zero except under the 36-month cap. Harmless, stale reasoning.

### lib/data/challenge.ts (653 lines)
- For: §3b match route: "have you seen Dr X?" then "what first name did you give them?", 3 attempts per (account, record), hint costs one, clinician release.
- Decides: `openChallenges` (131) only on a VERIFIED phone/email (153 to 155), skips claimed people (201), rejected/verified claims and spent budgets. `answerSeen` (243) no is final (status rejected). `answerName` (318) compares against `patients.first_name` only (378), counts failures atomically on `claim_attempts` (386), locks at 3 (401). `nameHint` (452) refused on the last attempt (482). `challengePassed` (531) needs `name_confirmed_at`. `releaseLock` (577) zeroes the budget, stamps `released_at`, expires the locked claim.
- Assumes: `nameMatches` in `./name-match`; callers put `accountId` from the session; `releaseLock` caller does the tenancy check via `getPatient` (comment 567).
- Promises: none of the 25 directly (claiming is under P4's "patient decides").
- Notes: see Broken for the re-lock after release, and the left join that can resurrect a rejected record. `challengePassed` is exported and has NO caller anywhere in app/components/lib (grep): the gate "every screen consults before rendering anything about a record" (511) is consulted by nobody. The consent step after the challenge is `verifyClaim`/ClaimFlow in claims.ts, which does not check it either.

### lib/data/checkins.ts (287 lines)
- For: the unprompted check-in channel (email/WhatsApp to patients), mutes, replies, admin counts.
- Decides: candidates are people with a non-deleted `patient_accounts` row (`candidates` 46, inner join 58), muted flag, last check-in via DISTINCT ON (73). `muteRate` (117) returns 0 on empty. `unmute` (189) uses DB `now()`. `checkinStats` (245) six integers only. No `lib/ai` import (header 12).
- Assumes: that an account row means the person claimed and consented (comment 39 to 41); the query does not check `people.claimed_at` or any consent flag, only that an account exists for that person id.
- Promises: none. Check-ins are Unclaimed (a), and their crisis routing is Unclaimed too (MAP Unclaimed 5).
- Notes: `candidates(limit = 500)` has no ORDER BY (66): past 500 accounts the same arbitrary subset is considered every run and the rest are never checked in on. Locale is hardcoded "en" (98), so an Arabic reader gets English check-ins. `muteRate` counts one person twice if they have two accounts (only one live account per person is allowed by `patient_accounts_person_unique`, so fine).

### lib/data/claims.ts (693 lines)
- For: §3 claiming: sprint 6 email/phone match route with a 6-digit code, and the clinician's invite link route; account follows the claimed record (22R).
- Decides: `suggestionsFor` (94) filters claimed and self. `startClaim` (200) refuses claimed people, one pending claim per (account, person, NULL patient), code hashed, 30 min TTL (61). `verifyClaim` (296) checks code hash, expiry, account; transaction marks verified, stamps `people.claimed_at` guarded on NULL (354 to 361), expires other pending claims, `bindAccountToPerson` (148) re-points the account only if its own person has no clinical rows. `applyClaimDecision` outside the tx (392); webhook `notifyRecordClaimed` (409). `issueInvite` (442) 24 random bytes, hash stored, 30 days. `redeemInvite` (545) single use via conditional UPDATE, checks `took` (580). `revokeInvite` scoped to issuer.
- Assumes: `app/(patient)/patient/claim/actions.ts:97` `sendClaimCode(personId)` passes a client-supplied personId and sends the code to `actor.email` (119), i.e. to the claimer's own already-verified inbox.
- Promises: P4 ("the patient decides who may read the history"): step 7 default off is kept (no default parameter, 300; `components/patient/claim-flow.tsx:45` unchecked). The mis-claim protection behind P4 is broken (see Broken).
- Notes: `verifyClaim` ignores the result of the `people` update (354): if somebody else claimed the person between code and confirm, the claim still goes verified, the account is re-pointed and the grant decision is re-applied. `patient_accounts_person_unique` (`drizzle/0034_patient_accounts.sql:61`) makes the re-point fail and roll back when the winner's account already sits on that person, so the race only lands when the winner's account was "kept" on its own person. `redeemInvite` does not expire other pending `person_claims` on the person (verifyClaim does, 370). No attempt limit on wrong codes in `verifyClaim`; `startClaim` can re-issue freely. Header (43) still describes the match route as "show a redacted name", which `challenge.ts:63` says was "one question too generous"; ClaimFlow still shows it (`components/patient/claim-flow.tsx:179`).

### lib/data/clinic-admin.ts (968 lines)
- For: everything that changes a clinic: enquiry (held), admin activation, first manager, invite/accept a clinician, existing-account join, clinician leaving, manager sign-in, region, admin list.
- Decides: `applyToClinic` (51) creates a `held` org and nothing else. `setClinicState` (134) refuses a solo org, touches nothing clinical. `inviteClinician` (212) one at a time, supersedes a live invite, 32-byte token hashed, 14 days. `resolveInvitation` (324) requires clinic `active` and stamps `terms_shown_at`. `acceptInvitation` (376) creates an `unverified` therapist and `takeSeat` (466). `joinWithExistingAccount` (504) refuses an account whose org holds any patient (566), claims the invitation, `takeSeat` with `ownOrganizationId`, moves the user and the verification row, returns the old org for the caller to cancel at period end. `removeClinician` (677) revokes meeting connections, creates a new solo org, `releaseSeat` (769), reparents the user; records stay with the clinic. `checkClinicPassword` (791) constant message for wrong address/password/held clinic. `clinicsForAdmin` (927) counts clinicians, lists managers.
- Assumes: callers are behind admin / clinic-admin guards and audit; `lib/billing/seats.ts` for seat billing; `getClinicActor` checks `clinic_state = 'active'` (comment 126).
- Promises: C1 (seat added): `takeSeat` on accept; the radar half is elsewhere. C4 (seat leaves: next bill one lower, clinician keeps working on PAYG): `removeClinician` releases the seat and gives a solo org with no subscription, so kept as far as this file goes; "the clinic keeps paying for the seat until the period ends" (764) is consistent with "next bill".
- Notes: `acceptInvitation` does not re-check that the clinic is still `active` (only `resolveInvitation` does). Passwords here are length-12 only (173, 382), not `validatePassword` as for back office. A double accept leaves a spare `users` row (451 to 454, logged, not cleaned). `joinWithExistingAccount` assumes the existing account is solo: an account currently inside ANOTHER clinic with no patients on that clinic's org is moved without `releaseSeat` on the old clinic, and the caller is told to cancel the OLD org's subscription, which would be the other clinic's. `removeClinician` leaves that clinician's patients in the clinic org with `therapist_id` pointing at somebody who is no longer there (Unclaimed c).

### lib/data/clinic-export.ts (164 lines)
- For: clinic CSV exports of the schedule and the bills, watermarked and audited.
- Decides: `export` capability required (80, 125), rows come only from `clinicSchedule` / `clinicBills` (no own query), watermark rows (50), audit `phi_access` for schedule export (92), withheld counts written as "withheld" (156).
- Assumes: `clinicSchedule` shortens names; caller passes the manager's email for the watermark.
- Promises: C2: the schedule export writes a patient name (first name plus last initial) and a time per appointment, with the clinician's name beside it (105 to 113). C2 as worded ("no patient name on any screen", "no caseload count") is broken; the code follows the C327 ruling instead.
- Notes: `cell()` (34) escapes quotes and commas but not a leading `=`, `+`, `-`, `@`: a guest name typed on a public join link (`clinic.ts:390`) lands in a spreadsheet as a formula (CSV injection).

### lib/data/clinic-team.ts (416 lines)
- For: clinic staff (the seventh principal): custom roles (max two slots), staff accounts, assignments to clinicians, revoking clinic sessions.
- Decides: `createRole`/`updateRole` (86, 140) through `roleProblem` with `creatorHolds: ADMIN_CAPABILITIES`; slot claimed by insert on a unique index. `removeRole` soft. `addStaff` (260) always `role: "viewer"` plus a role of this practice. `setAssignments` (319) refuses the admin, keeps only this practice's therapists (353 to 371), delete then insert (not in a transaction). `revokeClinicSessionsFor` (403).
- Assumes: `requireClinicAdmin` is the only door (comment 96); `getClinicActor` re-reads capabilities per request.
- Promises: A5 is about our staff; nothing here audits (callers may).
- Notes: there is no function to remove a staff member (no `deletedAt` write on `clinic_managers` in this file); grep before calling it missing. `staffFor` returns assignment rows by `organization_id` of the assignment, so after `removeClinician` a staff member's assignment to a departed clinician still lists that clinician's name (the row is never deleted).

### lib/data/clinic-visibility.ts (126 lines)
- For: telling the patient which practices can see their appointments and how their name appears (C327, C354), and the "works in a practice" label on radar cards.
- Decides: `clinicVisibilityFor` (50) clinic orgs only, name through `shortenForClinic`; `markClinicVisibilityShown` (89) first-time stamp; `clinicAffiliations` (107) one query, 200 cap.
- Assumes: `shortenForClinic` in `clinic.ts:279` is the same function the schedule uses (true).
- Promises: C2: this file exists because the clinic DOES see "first name plus last initial" (header 11 to 17). It is the disclosure of the breach of C2 as worded. Kept for the patient's side of C327.
- Notes: `asTheySeeIt` uses only the first row (69): a person held under two spellings at two clinics is shown one of them.

### lib/data/clinic.ts (713 lines)
- For: THE CLINIC WALL: every read a clinic principal may make (clinicians, invitations, schedule, earnings, usage, bills).
- Decides: `refuseWithout` (126) throws `ClinicRefused`; never-delegable capabilities checked against the role. `scopeToAssigned` (418) null for admin, `[]` means nobody. `clinicSchedule` (296): per session, patient first name plus last initial, guest name shortened the same way, clinician full name, `scheduled_at`, `status`; audited `phi_access` per read (354). `clinicEarnings` (462) per clinician net earnings and payout log without bank details. `clinicUsage` (563) weekly, whole clinic, `applyActivityFloor`. `clinicBills` (630) monthly totals with line kinds summed, count withheld under the floor.
- Assumes: `lib/clinic-auth/capabilities.ts` for `can`, `NEVER_DELEGABLE`, `THERAPIST_SCOPED`; `lib/data/sponsors.ts` `applyActivityFloor`; invoice lines kinds `platform` / `ai` (`lib/billing/plans.ts:248 to 256`).
- Promises: C2 broken as worded: `clinicSchedule` hands the clinic a patient name per row with the clinician and time (319 to 326, 388 to 391), so counting rows per clinician over a month is a caseload count, and the header (30 to 31) states it on purpose ("a list of patient names with appointment times"). MAP contradiction 1 is settled by the code: README is right, C2 is not. C5 (earnings per clinician, patients not): `clinicEarnings` kept. C3 (one bill, priced per seat): `clinicBills` reads only invoices with `session_id IS NOT NULL` (664), so the seat invoice is not on this screen at all; see Suspect. E1/E2 not here.
- Notes: see Broken for `total_cents` doubled by the line join. `status` includes cancelled; whether `sessions.status` has a no-show value was not in my slice (header says `no_show_at` excluded). The sponsor floor setting is shared (`settings.sponsor.activityFloor`).

### lib/data/copilot-view.ts (96 lines)
- For: one loader for everything `CopilotChat` renders (thread, patient name, quota, voice, last 100 messages), shared by `/copilot/<id>` and the patient profile.
- Decides: `copilotViewFor` (61) treats `getOrCreateThread` returning null as not found (the scope check).
- Assumes: `getOrCreateThread` scopes by organisation plus `patients.therapist_id` (copilot.ts:51 to 57). It does NOT consult the patient's grant; the view renders history regardless of grant state.
- Promises: T5: this is a read of past answers, not a question; no grant check here (see copilot.ts).
- Notes: creating a view creates a thread row as a side effect (a page load writes). No audit of this read here; `auditThreadRead` is only called on ask.

### lib/data/copilot.ts (530 lines)
- For: copilot threads per patient: scope, messages, quota, reply language, standing corrections, session notes into the thread, reset.
- Decides: `scope` (31) org plus therapist (super_admin: org only). `getOrCreateThread` (47) patient must be in the actor's org and, unless super_admin, the actor's own patient; one thread per patient (unique on `patient_id`). `checkQuota` (214): earned = max(`unclaimedPatientCredits`, completed sessions in window times `messagesPerPatientPerSession`), used = therapist messages with `session_id IS NULL` in the window. `addGuidance` appends, cap 4000 (355). `resetCopilotConversation` (484) deletes therapist/copilot/correction messages, keeps `session_note`, clears guidance, audits.
- Assumes: the grant check is done by the caller: `app/(app)/copilot/actions.ts:55` `accessFor(actor, patientId)` on EVERY `askCopilot`, before quota; `lib/ai/case-copilot.ts:163` narrows the prompt in the revoked state.
- Promises: T5 partly kept: the grant is re-read on every question (actions.ts:55), so a revocation takes effect on the next question. But `capabilities.copilot` is false only for "no relationship" and `unclaimed_bare` (actions.ts:49 to 53): a REVOKED clinician can still ask, answered from their own sessions under a prompt rule. "A revoked grant stops it" is not what the code does; it narrows it. Citations are stored (`citations`, 150).
- Notes: the thread's `therapist_id` is fixed at creation and the thread is unique per patient (62 to 68). If a patient is moved to another clinician in the same org, `getOrCreateThread` returns the old thread, but `getMessages` and `checkQuota` scope on `copilot_threads.therapist_id = actor` and return nothing / `{allowed:false, limit:0}`: the new clinician is told they "have used all 0 copilot questions" (Suspect, depends on whether reassignment exists). The question is appended before the model call (actions.ts:111), so a failed answer still spends a credit. Earned counts sessions by `created_at`, used counts messages by `created_at`: a session booked 13 months ago and completed this month earns nothing.

### lib/data/diagnoses.ts (136 lines)
- For: diagnoses proposed from uploaded documents (by `lib/ai/diagnoses.ts`) and the human confirm/reject; only confirmed rows count.
- Decides: `listDiagnoses` (42) all statuses with source sentence and open content flags; `confirmDiagnosis` (88) conditional on `status = 'proposed'`, then audits; `confirmedDiagnoses` (127) for the copilot.
- Assumes: caller checks the clinician may write this person (`app/(app)/patients/[id]/documents/actions.ts:201` `writable(patientId)`).
- Promises: P3 in spirit (a machine proposal is never shown as a clinician's finding); journal/record rules README lists ("nothing machine-written reaches you unsigned") kept by the status gate.
- Notes: see Broken: `confirmDiagnosis` audits with `organizationId: ""` (116), which `lib/audit.ts:108` writes into the uuid `organization_id` column (`?? null` does not catch an empty string), so the audit insert throws AFTER the status update committed.

### lib/data/discover.ts (346 lines)
- For: the patient home screen directory: explore rail, top rated, categories, search.
- Decides: `listable` (81) active therapist, verification `approved`, radar `suspended_until` null or past (112); no heartbeat filter; `present` (167) online only if status online AND heartbeat within `HEARTBEAT_STALE_MS`. `exploreTherapists` (219) online first then a day-seeded rotation, never rating. `topRated` (249) only at or above `RATINGS_VISIBLE_AFTER`. `search` (312) over specialties, languages, headline; never names.
- Assumes: `therapistRatings` and `RATINGS_VISIBLE_AFTER` from `feedback.ts`; `HEARTBEAT_STALE_MS` from `radar.ts`; radar suspension written by feedback (`suspended_until`).
- Promises: MAP Suspect 2 (demo is a label, never a decision): KEPT in this file. `demo` is selected (130) and passed through to the card (191, 268, 338) and appears in no WHERE, sort or filter. The old `SIMULATION_RUNNING ? undefined : demo = false` is gone (comment 91). C1 (added seat on radar the same hour): a newly accepted clinic clinician is `unverified` (clinic-admin.ts:428) and `listable` requires `approved`, so "on the radar the same hour" holds only once verification is approved; the promise's "with their verification state on the row" is not something this directory can show, because unverified clinicians are never listed here.
- Notes: the doc comment on `listable` (74) still says "not a fixture", which it no longer checks. `RATING_BAR` comment (345) says "used by the count query above"; there is no count query in this file. `listableRows` has `limit(500)` with no ORDER BY (136): past 500 clinicians an arbitrary set is invisible to explore, categories and search. `search` matches the specialty CODE (321) while categories show the taxonomy LABEL, so typing a label a patient just read may not match.

### lib/data/documents.ts (675 lines)
- For: a person's own documents (typed, dictated, uploaded), chunking with stable `[D7:3]` citations, flags, the extraction worker, the stream route's owner lookup.
- Decides: nothing about consent (header 44: every caller goes through `accessFor`; the stream route checks its own). `nextOrdinal` (178) inside the insert. `storeBlob` (182) random 24-byte path, Vercel blob `access: "public"` (209), local `.uploads` fallback. `writeChunks` (232) delete then insert. `documentContext` (370) flagged passages included and marked, `before` bound for the in-room copilot (C373). `raiseFlag` (443) checks the target belongs to the person. `withdrawFlag` (490) only the raiser. `extractPending` (554) bounded, oldest first.
- Assumes: `lib/data/grants.ts` `accessFor` at every caller; `app/api/documents/[id]` checks consent before streaming; H14 (the blob URL is a secret, never handed to a browser).
- Promises: P4 (one record, every version under its author): documents carry `uploadedByUserId` / `uploadedByAccountId` (96, 148). T5 citations: the `[D:n]` scheme is what makes "the sentence it came from attached" possible.
- Notes: `raiseFlag` by a clinician audits with `organizationId: ""` (477), the same defect as `diagnoses.ts:116` (see Broken). `auditDocument` falls back to `organizationId ?? ""` (614) whenever a clinician caller omits the organisation. `withdrawFlag` with neither id compares a uuid column to "" (504), which Postgres refuses as invalid input rather than matching nothing. The header says "a deleted document leaves a hole", but there is no delete in this module. `extractPending` takes no lock: two overlapping cron runs extract the same rows twice (chunks are delete-then-insert so the result converges, the cost is doubled).

### lib/data/ehr.ts (441 lines)
- For: EHR (SMART on FHIR) connections owned by an organisation: begin, store sealed tokens, revoke and sever, refresh, screen listing, launches, filing log.
- Decides: `beginConnection` (34) refuses without token sealing. `completeConnection` (63) revokes the old live one first. `revokeConnectionsFor` (126) severs launches (nulls FHIR ids) BEFORE revoking and clearing tokens, audits. `liveConnection` (191) refreshes with 60 s headroom; no refresh token or a refused refresh REVOKES the connection (226, 239). `connectionsFor` (263) no token columns. `patientForFhirId` (315) scoped to the connection, excludes severed. `writebacksFor` (344) note id, state, clinician name, dates. `filersOn` (404) distinct approving clinicians.
- Assumes: DB constraint `ehr_connections_revoked_holds_no_secret`; callers check the organisation belongs to the actor (clinic portal `app/(clinic)/clinic/records/page.tsx` reads `connectionsFor`, `filersOn`, `writebacksFor`).
- Promises: C2 partly: the clinic portal's records page reads `writebacksFor`, one row per filed note with the approving clinician's name and a timestamp (354 to 358). Grouped by clinician that is a count of documented sessions per clinician, i.e. a caseload-activity figure on a clinic screen, outside the C262 activity floor that `clinicUsage` applies. No patient name is selected.
- Notes: a single refused refresh (239) revokes and severs every launch permanently, so a transient token-endpoint error at the hospital deletes every FHIR mapping and the practice must reconnect and re-match every patient. Header comment (30) says a solo clinician reaches this from settings; `isClinicOrganization` (431, 67.1) suggests records connections are for clinics only; which is true is decided by the callers.

### lib/data/enrolment-verify.ts (365 lines)
- For: the 6-digit code sent to a work address to prove a `domain_email` enrolment, the periodic re-verification pause, the operator's one-step unpause and its queue.
- Decides: `sendEnrolmentCode` (63) spends earlier codes, `randomInt` code, 30 min, email only (`phone: null`, 112), text names no therapy (116). `confirmEnrolmentCode` (131) throttled 10 per 15 min per caller, scoped by `enrolments.person_id` in the WHERE, attempt counted before comparison, success sets `last_verified_at` and clears `paused_at` together. `pauseUnverified` (234) per sponsor window from `verify_cycle_started_at`, pauses stale rows, one notice with no employer named, rolls the window. `unpause` (305) one UPDATE. `pausedBenefits` (341) paused rows only, with the person's own name and email and the sponsor name, for our admin.
- Assumes: `sendEnrolmentCode` is called only from `enrol` (true: grep finds one caller, `lib/data/enrolment.ts:483`); the admin page is behind a staff guard (`app/(admin)/admin/benefits/page.tsx:40`, `actions.ts:43`).
- Promises: E1/E2 (sponsor never learns who or when): nothing here writes a sponsor-visible row. The C256 claim (the roster date carries no signal because everybody is paused in one run) is only half true: `confirmEnrolmentCode` stamps `last_verified_at = now` (200) at the person's own moment, and an `id_number` enrolment is stamped at creation (`enrolment.ts:435`), so `last_verified_at` is a per-person join/verify date. Whether a sponsor screen shows it is decided in `lib/data/sponsors.ts` (not my slice).
- Notes: see Broken: there is no way to get a second code. The header's remedy ("they re-enter their work address, which mints a new code", 34 to 35) cannot happen. `unpause` writes no audit (caller may). `pausedBenefits` shows never-verified enrolments? No: it filters `paused_at IS NOT NULL` (362), and a `domain_email` enrolment that never verified has `paused_at` null and `last_verified_at` null, so it is on nobody's queue.

### lib/data/enrolment.ts (558 lines)
- For: joining a sponsor's benefit with a printed code plus an identifier matched by shape or domain; hashing; primary pot choice; the person's own benefit list.
- Decides: `hashIdentifier` (98) salted with `AUTH_SECRET` and the sponsor; `hashIdentifierGlobal` (126) across sponsors. `lookupCode` (159) one sentence for every failure. `matchesGate` (222) whole-domain match or an anchored admin regex. `enrol` (272) throttled per caller+code (8 per 15 min), spike counter per code (count only), domain must be PROVED (364 to 381), insert with `last_verified_at` set now for `id_number` and null for `domain_email` (435), every DB refusal one message (449), `benefit_started` notice, code sent to the work address, attestation row for C265 (502). `setPrimarySponsor` (520) one UPDATE with CASE. `myBenefits` (544).
- Assumes: `payFromPot` requires `last_verified_at IS NOT NULL` (comment 428 to 433, in billing, not my slice); `enrolments_identifier_unique` is GLOBAL on `identifier_hash` and not partial on `removed_at` (`drizzle/0072_corporate.sql:195`), `enrolments_identifier_global_unique` (`drizzle/0085_identifier_once_ever.sql:29`), `enrolments_person_sponsor_unique` partial on not removed.
- Promises: E1/E2: KEPT for this file: `enrol` writes nothing a sponsor reads and notifies no sponsor (258 to 263); the only sponsor-visible trace is the spike COUNT per code (314). What the sponsor learns is decided by `sponsors.ts`. E4 not here.
- Notes: the `benefit_started` notice (461) is written for a `domain_email` enrolment that is not funded until the code is answered, so the person's own log says the benefit started when it has not (P2 wording risk). `setPrimarySponsor` with an enrolment id that is not theirs sets every one of their rows to not primary and still returns ok (527 to 534), leaving no primary pot. `matchesGate` compiles an admin-typed pattern per attempt (243) with no guard on catastrophic backtracking. "Once, ever" (C246) means a guesser who enrols first with a real colleague's id number locks that colleague out permanently, with the one message "That could not be activated" and no route to a person.

### lib/data/export.ts (1129 lines)
- For: the patient's full record extract: a tokenised link emailed to the patient, assembled at read time, rendered as one self-contained HTML page plus JSON, with a 12-character verification code a third party can check.
- Decides: `requestPatientExport` (102) clinician scoped to own org and own patient, super_admin across orgs; 4 per hour per patient (139); revokes the previous link; token 32 bytes hashed; notifies the owning clinician if somebody else pressed (199); alerts up to 10 super_admins with patient name, address and code (217, 237). `openExport` (273) one outcome for unknown/expired/revoked, counts opens. `buildExport` (307) every chart of the PERSON (349), sessions with approved-only note text (554, C372), provenance, off-record seconds, risk level and action (380, 574), transcripts (399, capped at 60,000 segments), signers with licence, summaries, journals, homework, confirmed diagnoses only (507). `verifyExtract` (957) returns date and counts only. `requestOwnExport` (1032) email only, `phone: null` (1110), 4 per hour.
- Assumes: callers pass the right email. Checked: patient path `app/(patient)/patient/record/actions.ts:24` passes `actor.email`; admin path `app/(admin)/admin/actions.ts:587` super_admin with a reason; console path `app/(admin)/admin/tv/actions.ts:43` `requireElevated`.
- Promises: P3 (nothing machine-written reaches you unsigned): partly. Draft note text is withheld (554). But `risk_assessments` rows are written by keyword or model detection (`lib/crisis/alerts.ts:383`, `source: "keyword" | "model"`), are never signed, and the patient's own extract prints "risk noted: <level>" (678 to 680) and the JSON carries `riskAction`, the recommended action written for the clinician (575). A5 (every read written down): export requests are audited (184, 1091) but `openExport` writes no audit row, only `open_count`.
- Notes: what leaves and to whom: (1) the patient's own inbox (the chart email, typed by a clinician and never verified, for the admin paths; the account email for the patient path); (2) a copy of the SAME live link to the operator: `app/(admin)/admin/tv/actions.ts:51` passes `copyTo: actor.email`, which `lib/mail.ts:470` sends as `bcc`. The operator can open the patient's full record, contradicting this file's header (50 to 52: "Whoever pressed the button never sees the contents") and the notices this file writes to the clinician (204) and to staff (256) ("Nobody here read it"). (3) the admin alert carries the patient's full name and address to every super_admin (218 to 220). `mailClinicianHistory` (tv/actions.ts:79) sends a clinician's "session and note history" CSV to any typed address; its content is `lib/console/history.ts` (not my slice). The session query left-joins `risk_assessments` (385): a session with two risk rows (outside the 10-minute dedup in alerts.ts) appears twice in the extract and in `verifyExtract`'s count (990 has no risk join, so the page and the verifier disagree). The one clinician-side door the header describes ("a clinician may only do this for their own caseload", 98) has no caller in `app/`.

### lib/data/facts.ts (536 lines)
- For: the clinical evidence layer: record a fact with its quote and evidence, read ranked current facts, contested fields, evidence back to the source line, verify/dispute/supersede.
- Decides: `recordFact` (146) throws `JournalInferenceError` when evidence is a journal and the domain is `diagnosis` or `risk` (127, 158), BEFORE any DB call; priority derived from source (173); confidence only for `ai` (177). Routed by `regionOfPerson` (162), not pinned. `factsFor` (201) active and disputed only unless `all`; ranking in TS via `rankFacts`. `contestedFields` (288) JSON key, not a NUL. `evidenceFor` (337) returns two lines either side of a transcript segment. `verifyFact` (452), `disputeFact` (482), `supersedeFact` (512) each audited.
- Assumes: `drizzle/0068_journal_inference.sql:42 to 45` CHECK `facts_journal_never_concludes` (evidence_kind <> 'journal' OR domain NOT IN ('diagnosis','risk')), and the 0062 constraints (non-blank evidence, AI never born verified, no lower-rank supersede, value immutable); callers check grants.
- Promises: "journal cited, never concluded" (README invariant, MAP line 65): KEPT for the two named domains, in code (158) and in the database. It is narrower than its own comment (96 to 98: "may never produce ... any statement phrased as a conclusion about the person"): a journal can still be the evidence for a fact in `presentation`, `function`, `social`, `history`, `medication` (the other domains in `lib/clinical/currency.ts:26 to 54`), and `verifyFact` lets a clinician verify such a row, after which "the product will state it plainly" (448). T5 citations: `evidenceFor` is the "sentence it came from".
- Notes: `verifyFact` and `disputeFact` do not check that the UPDATE matched a row (459, 490) and audit regardless, so an id from another person writes an audit row for a verification that did not happen. `verifyFact` does not stop verifying a `disputed` fact or the verifier being the AI's own caller.

### lib/data/feedback.ts (918 lines)
- For: the patient's rating page (reached by the session's feedback token), arrival rating, reports, public ratings, off-record gaps and note provenance, the radar ban ladder, releasing the patient brief, rating reminders, and the automatic "patient left waiting" sweep that suspends clinicians.
- Decides: `feedbackContext` (99) 72-hour window (131), brief only when `patientStatus = 'approved'` (141). `submitFeedback` (180) upsert, email required, email written onto the patient and session if empty. `fileReport` (338). `therapistRatings` (402) completed ratings only; `RATINGS_VISIBLE_AFTER = 5` (384). `offRecordGaps` (476) gaps over 20 s between segments. `noteProvenanceFor` (510) consent granted AND segments exist, else `clinician`; gaps make it `partial`. `suspensionFor` (554) 24 h, 72 h, then ten years. `countNoShows` (560) all-time `actioned` no-show reports. `suspendFromRadar` (574) sets `suspended_until` and status offline. `releaseBrief` (615) patient fields only, once. `sweepUnratedSessions` (707) one reminder per session. `sweepAbandonedPatients` (796): any session with `patient_joined_at` older than 10 minutes, `started_at` null, status `scheduled`, and no report row, gets an automatic `actioned` no-show; the first is a warning, the second 24 h off the radar, the third 72 h, after that indefinite (856).
- Assumes: `patient_joined_at` means "sitting in the room now". It is stamped by `joinByToken` (`lib/data/sessions.ts:873`) whenever the patient submits their name on the join link, and `resolveJoinToken` (`lib/data/sessions.ts:833 to 838`) has no check against `scheduled_at`. The hourly `crisis` cron runs the sweep over every session (`app/api/cron/[job]/route.ts:137`), and the patient's own poll runs it for one (`app/join/[token]/actions.ts:564`).
- Promises: none of the 25 directly. C1 ("on the radar the same hour") is undone by this file for as long as a suspension lasts. T4 (invite link works) interacts: the same invitation link that stamps the join is what triggers the no-show (see Broken).
- Notes: task 122 (a clinician banned for three days): the code gives 72 hours on the THIRD automatic no-show (prior = 2, `suspensionFor(1)`), not the second as the header (547) says ("a day for the first, three for the second"); the first is only a warning. Counts never decay (all-time, 560). The suspension email always says "booked you on the Crisis Radar" (879), whether the session came from the radar or from an ordinary scheduled invitation. `submitFeedback` and `fileReport` do not apply the 72-hour window that `feedbackContext` applies; a token keeps working for writes forever. `fileReport` is unlimited per token. `releaseFromRadarBan` has one caller (`app/(admin)/admin/actions.ts:713`); there is no review queue for "indefinitely, pending review" in this file. The no-show session is left `scheduled` and paid; nothing here refunds the patient who waited.

## Stale

1. `lib/data/admin.ts:132 to 139`: "written to both places that store it". `drizzle/0083_verification_one_truth.sql:108 to 121` forces `users.verification_status` to the derived value on every UPDATE, so the direct write at 158 to 161 is overwritten; only the `therapist_verifications` upsert decides.
2. `lib/data/capital.ts:151 to 158`: says `monthRange` starts at the first thing in the PRODUCT and the founders' money predates it. `lib/data/actuals.ts:480` includes `capital_contributions` in the range, so the pre-range branch fires only under the 36-month cap.
3. `lib/data/challenge.ts:511 to 513`: `challengePassed` is "the gate every screen consults before rendering anything about a record". It has no caller in app/, components/ or lib/ (grep).
4. `lib/data/claims.ts:43 to 44` and `components/patient/claim-flow.tsx:174 to 180`: the match route "show[s] a redacted name" `H••••• A•••••`. `lib/data/challenge.ts:63 to 67` records that this was "one question too generous" and removed in sprint 13; the sprint 6 card still renders it.
5. `lib/data/enrolment-verify.ts:33 to 35`: "The fix is that they re-enter their work address, which mints a new code." Re-entering the same address hits `enrolments_identifier_unique` (`drizzle/0072_corporate.sql:195`) inside `enrol` and returns "That could not be activated" before any code is sent.
6. `lib/data/enrolment-verify.ts:222 to 225` (C256): "the date on every roster row is then the same date and carries no signal about anybody". `confirmEnrolmentCode` (200) and `enrol` for `id_number` (`enrolment.ts:435`) stamp `last_verified_at` at the person's own moment.
7. `lib/data/discover.ts:74`: "not a fixture" in the doc of `listable`; the demo filter was removed (comment 88 to 111).
8. `lib/data/discover.ts:345`: `RATING_BAR` "Used by the count query above"; no count query exists in the file.
9. `lib/data/feedback.ts:547 to 548`: "A day for the first, three for the second". Code (856): first is a warning, second 24 h, third 72 h.
10. `lib/data/export.ts:50 to 52`, 204, 256: "Whoever pressed the button never sees the contents" / "Nobody here read it". `app/(admin)/admin/tv/actions.ts:51` bccs the live link to the operator (`lib/mail.ts:470`).
11. `lib/data/export.ts:98`: "A clinician may only do this for their own caseload". No clinician-side caller of `requestPatientExport` exists in app/; only the two admin paths.
12. `lib/data/documents.ts:55 to 57`: "A deleted document leaves a hole". No delete function in the module.
13. `lib/data/facts.ts:96 to 98`: a journal "may never produce ... any statement phrased as a conclusion about the person". The code and CHECK cover two domains (`diagnosis`, `risk`) only.
14. `lib/data/checkins.ts:39 to 41`: "A claimed account means the person ... took ownership of their record and agreed to the consent question". The query (58, 65) only requires a live `patient_accounts` row for the person; it checks neither `people.claimed_at` nor any consent column.
15. `lib/data/clinic.ts:30 to 31` vs `docs/VALUE-STATEMENTS.md` C2: the file's own charter is "a list of patient names with appointment times". Not stale code, a stale promise (see Promise evidence C2).
16. `lib/data/ehr.ts:30 to 32`: "a solo clinician from settings" reaches the connection flow; `isClinicOrganization` (431) exists because records connections are for clinics (67.1). One of the two is stale; the callers decide.

## Suspect

1. `lib/data/clinic.ts:664` / C3: `clinicBills` reads only invoices with `session_id IS NOT NULL`. The per-seat invoice that C3 promises ("one invoice, priced per seat") is not on `/clinic/bills` (`app/(clinic)/clinic/bills/page.tsx:43` calls only `clinicBills`). If the seat invoice is shown nowhere in the clinic portal, C3 cannot be proved on a screen. Answer: `lib/billing/seats.ts`, `app/(clinic)/clinic/page.tsx`.
2. `lib/data/clinic-admin.ts:504 to 645`: `joinWithExistingAccount` assumes the joining account is solo. An account already inside another clinic (no patients on that org) is moved without `releaseSeat` there, and the caller is handed the OLD org to cancel at period end, which would be the other clinic's subscription. Matters for C3/C4 billing. Answer: the caller of `joinWithExistingAccount` and `lib/billing/seats.ts`.
3. `lib/data/copilot.ts:62 to 68, 31 to 38`: one thread per patient, `therapist_id` frozen at creation, reads scoped to the thread's therapist. If a patient is ever reassigned within a practice, the new clinician's quota reads `{allowed:false, limit:0}` and the history reads empty. Answer: whether `patients.therapist_id` is ever updated (grep `update(patients)`).
4. `lib/data/claims.ts:354 to 376`: `verifyClaim` ignores whether the `people` UPDATE matched. A pending match claim that outlives an invite redemption by another account (redeemInvite does not expire pending claims, 561 to 600) is verified anyway, re-applies `applyClaimDecision` with the loser's choice, and fires `record.claimed` again. `patient_accounts_person_unique` stops the account re-point only when the winner's account was moved onto the person. Answer: `lib/data/grants.ts` `applyClaimDecision` (does it overwrite the winner's decision?).
5. `lib/data/enrolment-verify.ts:200`, `lib/data/enrolment.ts:435`: per-person `last_verified_at` is a join or verify date. If `lib/data/sponsors.ts` puts it on the roster or any sponsor screen, E1 ("never when") is broken. Answer: `lib/data/sponsors.ts` roster select list.
6. `lib/data/ehr.ts:238 to 240`: any refused token refresh revokes the connection and severs every FHIR mapping permanently. A transient hospital error costs a practice every patient link. Answer: `lib/ehr/smart.ts` `refresh` (does it distinguish 400 invalid_grant from 5xx/network?).
7. `app/(admin)/admin/tv/actions.ts:79 to 131` `mailClinicianHistory`: a clinician's "session and note history" CSV to any typed address plus a copy to the operator. If `lib/console/history.ts` includes patient names, times or note text, this is the widest exfiltration door in the product. Answer: `lib/console/history.ts`.
8. `lib/data/export.ts:349 to 356`: the extract covers every chart of the PERSON. The admin paths deliver to the email typed on ONE chart by one clinician (`requestPatientExport` 147). If two clinicians' charts can share an unclaimed person, one clinician's typo sends another practice's notes and transcripts to a stranger. Answer: `lib/data/people.ts` (can unclaimed patients rows share a person?).
9. `lib/data/checkins.ts:46 to 66`: candidates are any person with a live account. Whether an account can exist with a `person_id` before the person consented (self sign-up creates a person, per `claims.ts:98 to 110`) decides whether check-ins go to people who never agreed. Answer: `lib/checkins/` sender and the consent column on `people`.
10. `lib/data/assessments.ts:290 to 316`: `completeAssignment` scores partial answers. Whether the patient UI allows "finish" before the last question is decided in `components/`; the server allows it.
11. `lib/data/enrolment.ts:520 to 534`: `setPrimarySponsor` with a foreign enrolment id leaves the person with no primary. What `payFromPot` does with no primary decides whether a covered patient silently pays full price. Answer: `lib/billing` `payFromPot`.
12. `lib/data/feedback.ts:837 to 846`: the no-show session stays `scheduled` and paid; whether the patient who was left waiting is refunded or told anything is not here. Priority 4 (stuck). Answer: `lib/data/radar.ts`, `lib/billing`.

## Broken

1. **Any signed-in patient can take ownership of a record matched on their phone or email without passing the §3b challenge.** `app/(patient)/patient/claim/page.tsx:79 to 105` renders both `ClaimChallenge` (the two questions) and `ClaimFlow` (sprint 6) for the same person when the phone matches (`suggestionsFor` matches phone and email, `lib/data/claims.ts:113`). ClaimFlow's "Yes, send code" calls `sendClaimCode(s.personId, "email")` (`components/patient/claim-flow.tsx:196`), which sends the code to the CLAIMER's own address (`app/(patient)/patient/claim/actions.ts:119`), and `verifyClaim` (`lib/data/claims.ts:296`) then claims the whole person (every clinician's chart, `patientsMoved`) with no check of `challengePassed`, `seen_therapist` or `name_confirmed_at`. The code proves only the inbox the person already proved. The mother-and-daughter-on-one-handset case `challenge.ts:35 to 39` exists to stop succeeds in one tap plus a code to the daughter's own email, and the card shows the owner's initials first. `startClaim` also does not check the personId is one of the caller's suggestions (200 to 216). Grepped for a patch: `challengePassed` has no caller.
2. **The clinic's bill total is overstated for every AI-consented session.** `lib/data/clinic.ts:660 to 662`: `SUM(i.amount_cents - i.discount_cents)` over `invoices LEFT JOIN invoice_lines` counts each invoice once per line; a consented session has two lines (`lib/billing/plans.ts:248 to 256`), so its total is added twice. The practice manager sees a Total larger than platform fee plus AI fee on `/clinic/bills`, and the CSV export (`clinic-export.ts:159`) carries the same wrong figure. Zero-priced (subscribed) sessions hide it.
3. **Confirming or rejecting a proposed diagnosis errors after it has already been saved.** `lib/data/diagnoses.ts:116` passes `actor: { userId, organizationId: "" }`; `lib/audit.ts:108` writes `input.actor?.organizationId ?? null`, which keeps `""`, into the uuid `audit_log.organization_id`; Postgres refuses it and `audit()` is allowed to throw (60 to 64). The status UPDATE at 96 to 111 has committed, so the clinician sees an error, presses again, and is told "That has already been decided." Same defect in `lib/data/documents.ts:477` (`raiseFlag` by a clinician: the flag is stored, the action throws) and potentially 614.
4. **A patient who opens an invitation link early gets their clinician an automatic no-show, and on the second and third a radar suspension.** `lib/data/sessions.ts:873` stamps `patient_joined_at` when the patient submits their name, with no check against `scheduled_at` (`resolveJoinToken` 833 to 838; the join page only shows a wait counter after the scheduled time, `app/join/[token]/page.tsx:218`). `sweepAbandonedPatients` (`lib/data/feedback.ts:807 to 829`) takes any `scheduled` session whose `patient_joined_at` is older than 10 minutes and not started, and the hourly `crisis` cron runs it over every session (`app/api/cron/[job]/route.ts:137`). Opening a Thursday invitation on Tuesday files an `actioned` no-show on the next cron; the clinician is emailed that a patient "booked you on the Crisis Radar ... waited 10 minutes" (879), and the second and third such events take them off the radar for 24 h and 72 h. This is a strong candidate for task 122's three-day ban. `patient_joined_at` is also re-stamped on every rejoin.
5. **A second lockout after a clinician's release is invisible to that clinician.** `lib/data/challenge.ts:589 to 602` sets `released_at`; a later third wrong name sets `locked_at` again (409 to 418) but never clears `released_at`; `lockedOn` (635 to 648) requires `released_at IS NULL`. The therapist's screen says nobody is locked out while the patient reads "Ask your therapist to send you an invite link." (The partial index `drizzle/0045_two_handles.sql:96` has the same predicate.)
6. **A "no" to "have you seen this therapist?" is not final once any other claim row exists for that record and account.** `openChallenges` (`lib/data/challenge.ts:182 to 188`) left-joins `person_claims` on (patient, account) without a status filter, and `releaseLock` retires a locked claim to `expired` (621 to 627). A record with an `expired` row and a later `rejected` row yields two result rows; the `expired` one passes the filter at 213 and the record is offered again at stage "name" (`seenTherapist` true), contradicting 57 to 59 and 237 to 241.
7. **A work-email benefit whose code expired, bounced or was mistyped has no way to get a new code.** `sendEnrolmentCode` is called only from `enrol` (`lib/data/enrolment.ts:483`); there is no resend action (`app/(patient)/patient/benefit/actions.ts` exports `checkCode`, `activateBenefit`, `confirmCode`, `choosePrimary`, `askAboutEmployer`). Re-entering the address fails on `enrolments_identifier_unique` (global, not partial) with "That could not be activated." The enrolment sits with `last_verified_at` null and `paused_at` null, so it funds nothing (per `payFromPot`) and is not in the operator's `pausedBenefits` queue (`enrolment-verify.ts:362`). Stuck, nobody told.
8. **The patient's own record extract carries unsigned machine risk output.** `lib/data/export.ts:380 to 381, 574 to 575, 678 to 680`: `risk_assessments.level` and `recommended_action` (written by keyword or model detection, `lib/crisis/alerts.ts:383 to 396`, never signed) appear as "risk noted: high" in the HTML and as `riskAction` (an instruction to the clinician) in the JSON. P3. The same left join duplicates a session in the extract when it has two risk rows more than 10 minutes apart (`lib/crisis/alerts.ts:307`).
9. **The console's "send one person their own record" gives the operator the live link.** `app/(admin)/admin/tv/actions.ts:51` `copyTo: actor.email` becomes `bcc` in `lib/mail.ts:470`; the URL is the working token. The operator can open notes and transcripts, while the clinician is told "Nobody here read it" (`lib/data/export.ts:204`). `openExport` (273) writes no audit row, so the operator's open is recorded only as an `open_count` increment indistinguishable from the patient's.
10. **Clinic CSV export is formula-injectable.** `lib/data/clinic-export.ts:34 to 37` does not neutralise a leading `=`, `+`, `-`, `@`; the "Who" column includes guest names typed by anybody holding a join link (`clinic.ts:390`), which open as formulas in the practice's spreadsheet.

## Looks broken, is handled

1. `lib/data/capital.ts:318` passes `resourceId: "kind:month"` to `audit()` (H6: uuid column). Handled at `lib/audit.ts:129 to 130`, which routes non-uuids to `resource_key`.
2. `lib/data/admin.ts:259 to 370` hands patient names, emails, phones and session times to the console with no audit inside the file. Handled at `app/(admin)/admin/therapists/[id]/page.tsx:40 to 55`: `requireRole("super_admin")` and a `break_glass` audit written before the reads.
3. `lib/data/copilot.ts` never checks the patient's grant. Handled per question at `app/(app)/copilot/actions.ts:55 to 61` (`accessFor` before quota) and in the prompt at `lib/ai/case-copilot.ts:163`.
4. `lib/data/assessments.ts:125` `assignInstrument` takes a patientId with no access check. Handled at `app/(app)/patients/[id]/assessments/actions.ts:27 to 41` (`getPatient` plus `accessFor` not revoked).
5. `lib/data/claims.ts:148 to 179` `bindAccountToPerson` could re-point a second account onto an already-claimed person. Mostly handled by `patient_accounts_person_unique` (`drizzle/0034_patient_accounts.sql:61`), which aborts the transaction (see Suspect 4 for the gap).
6. `lib/data/facts.ts:158` journal-conclusion rule looks like a service-only check. Also enforced by CHECK `facts_journal_never_concludes` (`drizzle/0068_journal_inference.sql:42 to 45`).
7. `lib/data/clinic-admin.ts:677` `removeClinician` revokes meeting connections but not EHR connections. Handled by construction: `ehr_connections` has no user column and `liveConnection` is keyed on organisation (`lib/data/ehr.ts:191`), as the comment at 722 to 739 argues.
8. `lib/data/diagnoses.ts:42` / `documents.ts` take a bare personId with no actor. Handled by callers going through `writable()` (`app/(app)/patients/[id]/documents/actions.ts:180, 201`) and `accessFor` (documents header 44 to 50).

## Unclaimed

(a) worth selling, nothing advertises it:
- `lib/data/export.ts:957` `verifyExtract`: a third party can check a record extract is genuine by code, without seeing anything in it.
- `lib/data/assessments.ts:155 to 169, 425`: per-answer timing on validated questionnaires (the "ninety seconds on item 9" signal).
- `lib/data/facts.ts:288, 337`: contradictions between sessions shown side by side, and every fact traceable to the transcript line with two lines of context.
- `lib/data/clinic-export.ts`: watermarked, audited clinic exports.
- `lib/data/checkins.ts`: unprompted check-ins with a measured mute rate that halts the channel.

(b) nobody should have it, a hole:
- The sprint 6 ClaimFlow (Broken 1): claim a whole person with a code to your own inbox.
- `app/(admin)/admin/tv/actions.ts:37, 79`: operator copy of a patient's live record link; a clinician's history to any address.
- `lib/data/clinic.ts:296` plus `lib/data/ehr.ts:344`: the practice manager sees who is coming, when, with whom, and per-clinician filing counts (C2).
- `lib/data/enrolment.ts:98 to 130` "once, ever": whoever enrols first with a colleague's id number owns that identifier permanently.

(c) half built, a lifecycle with no way out or a screen with no door:
- Clinic staff cannot be removed: `lib/data/clinic-team.ts` has no delete, and grep finds no other `update(clinicManagers)` except `lastSignInAt` (`clinic-admin.ts:828`). Removing their role leaves an account that signs in and sees nothing.
- A departed clinician's patients stay in the clinic org with `therapist_id` pointing at someone no longer there (`clinic-admin.ts:659 to 666, 774 to 777`); no function hands them to anyone, and the clinic cannot see them (C2).
- `challengePassed` (challenge.ts:531): a gate with no door.
- Enrolment code resend (Broken 7).
- "Indefinitely, pending review" radar bans (`feedback.ts:557`) with no review queue in this slice; only a per-clinician release in the admin actions.
- `requestPatientExport` for a clinician (export.ts:98): the function supports it, no screen calls it.

## Promise evidence

| Id | What in this slice | file:line | Verdict |
|---|---|---|---|
| P3 | Draft note text withheld from the extract; brief released only on `patientStatus = approved`; assessment score without band to the patient | export.ts:554, feedback.ts:141, 640, assessments.ts:286 to 332 | partly: machine risk level and recommended action reach the patient's extract unsigned (export.ts:574 to 575, 678) |
| P4 | Claim default off; every chart of a person in one extract; documents and facts keep author; summaries by version | claims.ts:300, claim-flow.tsx:45, export.ts:349, documents.ts:96 | partly: "the patient decides who may read" is kept, but whose record it is can be taken by the wrong person through ClaimFlow (Broken 1) |
| T1 | `noteProvenanceFor` marks transcript only when segments exist | feedback.ts:510 to 541 | kept for provenance (the "draft on every screen" half is not in this slice) |
| T2 | Off-record gaps derived from segment timeline, provenance `partial`, seconds in export | feedback.ts:476, export.ts:566 | cannot tell from here (depends on capture) |
| T4 | Join link stamps `patient_joined_at` with no time gate | sessions.ts:873 (read for context) | the link works, but using it early punishes the clinician (Broken 4) |
| T5 | Grant re-read on every question; citations stored | copilot/actions.ts:55, copilot.ts:150 | partly: a revoked clinician is narrowed, not stopped |
| C1 | Seat taken on accept; radar lists only `approved`; suspension removes | clinic-admin.ts:466, discover.ts:86, feedback.ts:574 | partly: an invited clinician is `unverified` and not listed until approved; "verification state on the row" cannot show on a directory that lists only approved |
| C2 | Schedule with patient first name plus initial per row; schedule export; EHR filing log per clinician | clinic.ts:296 to 395, clinic-export.ts:105, ehr.ts:344 | broken as worded (names on screen and in CSV; counting rows gives caseload). The code follows C327, which contradicts C2 |
| C3 | Bills screen reads session invoices only | clinic.ts:630 to 685 | partly / cannot tell: seat invoice absent from this screen (Suspect 1); total wrong (Broken 2) |
| C4 | Seat released, clinician gets a solo org and keeps working | clinic-admin.ts:677 to 781 | kept here (billing half in seats.ts) |
| C5 | Earnings per clinician, payouts log without bank details, no patient | clinic.ts:462 to 540 | kept |
| E1 | Enrolment writes nothing a sponsor reads; spike is a count | enrolment.ts:258, 314 | kept here; per-person `last_verified_at` is a latent "when" (Suspect 5) |
| E2 | No sponsor surface imports enrolment-verify | enrolment-verify.ts:334 | cannot tell from here (sponsors.ts) |
| A1 | Enrolment funds nothing until verified (claim about `payFromPot`) | enrolment.ts:428 to 435 | cannot tell from here |
| A5 | Capital and cost writes audited; admin clinician page audited; export requests audited; admin-team, setClinicState, unpause, openExport write none | capital.ts:213, 241, 313; export.ts:184, 1091 | partly |

## Coverage

| File | Lines | Status |
|---|---|---|
| lib/data/actuals.ts | 640 | read |
| lib/data/admin-team.ts | 80 | read |
| lib/data/admin.ts | 468 | read |
| lib/data/assessments.ts | 598 | read |
| lib/data/capital.ts | 326 | read |
| lib/data/challenge.ts | 653 | read |
| lib/data/checkins.ts | 287 | read |
| lib/data/claims.ts | 693 | read |
| lib/data/clinic-admin.ts | 968 | read |
| lib/data/clinic-export.ts | 164 | read |
| lib/data/clinic-team.ts | 416 | read |
| lib/data/clinic-visibility.ts | 126 | read |
| lib/data/clinic.ts | 713 | read |
| lib/data/copilot-view.ts | 96 | read |
| lib/data/copilot.ts | 530 | read |
| lib/data/diagnoses.ts | 136 | read |
| lib/data/discover.ts | 346 | read |
| lib/data/documents.ts | 675 | read |
| lib/data/ehr.ts | 441 | read |
| lib/data/enrolment-verify.ts | 365 | read |
| lib/data/enrolment.ts | 558 | read |
| lib/data/export.ts | 1129 | read |
| lib/data/facts.ts | 536 | read |
| lib/data/feedback.ts | 918 | read |
